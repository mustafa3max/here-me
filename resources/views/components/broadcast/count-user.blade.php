<div class="flex grow flex-col items-center justify-center gap-2 p-2 hover:bg-secondary-light dark:hover:bg-secondary-dark"
    title="{{ __('str.number_episode_' . $title) }}">
    <span>{{ $count }}</span>
    <i class="bi bi-{{ $icon }}"></i>
</div>
